# Postman

File:

- `postman/collection.json`
- `postman/environment.json`

Generator CRUD memperbarui collection dengan folder module, method, URL, header bearer token, body example, dan response example.

Import kedua file ke Postman lalu isi variable `token` setelah login.
